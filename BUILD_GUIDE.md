# OfflineMessenger APK 编译指南\n\n本指南将帮助您在本地编译 OfflineMessenger Android 项目生成 APK 文件。\n\n## 环境要求\n\n### 1. 系统要求\n- **操作系统**: Windows 10+, macOS 10.12+, 或 Linux\n- **RAM**: 最少 4GB（推荐 8GB）\n- **磁盘空间**: 最少 10GB\n\n### 2. 必须安装的工具\n\n#### JDK 11+\n```bash\n# Windows (使用 Chocolatey)\nchoco install openjdk11\n\n# macOS (使用 Homebrew)\nbrew install openjdk@11\n\n# Linux (Ubuntu/Debian)\nsudo apt-get install openjdk-11-jdk\n```\n\n验证 JDK 安装:\n```bash\njava -version\njavac -version\n```\n\n#### Maven 3.6+\n```bash\n# 下载地址: https://maven.apache.org/download.cgi\n# 或使用包管理器\n\n# macOS\nbrew install maven\n\n# Linux\nsudo apt-get install maven\n```\n\n验证 Maven 安装:\n```bash\nmvn --version\n```\n\n#### Android SDK\n需要通过 Android Studio 或单独安装。\n\n**推荐方式 - 使用 Android Studio**:\n\n1. 下载并安装 [Android Studio](https://developer.android.com/studio)\n2. 打开 Android Studio\n3. 进入 `SDK Manager` (Tools → SDK Manager)\n4. 安装以下组件:\n   - Android SDK Platform 31\n   - Android SDK Build-Tools 31.0.0\n   - Android SDK Tools\n   - Platform Tools\n   - Emulator (可选)\n\n**设置环境变量 (必须)**:\n\nWindows:\n```batch\nsetx ANDROID_SDK_ROOT C:\\Users\\YourUsername\\AppData\\Local\\Android\\Sdk\nsetx ANDROID_HOME C:\\Users\\YourUsername\\AppData\\Local\\Android\\Sdk\n```\n\nmacOS/Linux:\n```bash\nexport ANDROID_SDK_ROOT=~/Android/Sdk\nexport ANDROID_HOME=~/Android/Sdk\n```\n\n## 编译步骤\n\n### 步骤 1: 克隆项目\n```bash\ngit clone https://github.com/llw77122/OfflineMessenger.git\ncd OfflineMessenger\n```\n\n### 步骤 2: 安装依赖\n```bash\n# 构建整个项目（包括核心库）\nmvn clean install\n```\n\n### 步骤 3: 生成 APK\n\n#### 开发版本 (Debug APK)\n```bash\n# 编译 Android 模块\ncd android\nmvn clean package -Dandroid.release=false\n```\n\nAPK 将生成在: `android/target/offline-messenger-android-1.0.0-SNAPSHOT.apk`\n\n#### 发布版本 (Release APK)\n```bash\n# 需要签名密钥\nmvn clean package -Dandroid.release=true\n```\n\n### 步骤 4: 验证编译结果\n\n编译成功后，您会看到:\n```\n[INFO] BUILD SUCCESS\n```\n\n生成的 APK 文件位置:\n```\nandroid/target/offline-messenger-android-1.0.0-SNAPSHOT.apk\n```\n\n## 安装 APK 到设备\n\n### 连接设备\n确保您的 Android 设备:\n1. USB 调试已启用 (开发者选项 → USB 调试)\n2. 通过 USB 连接到电脑\n\n### 安装方法\n\n#### 使用 ADB (Android Debug Bridge)\n```bash\n# 列出所有连接的设备\nadb devices\n\n# 安装 APK\nadb install -r android/target/offline-messenger-android-1.0.0-SNAPSHOT.apk\n```\n\n#### 使用 Android Studio\n1. 打开项目\n2. 点击 \"Run\" → \"Run 'app'\"\n3. 选择您的设备\n\n## 常见问题解决\n\n### 1. \"ANDROID_SDK_ROOT not set\" 错误\n**解决方案**: 确保已设置环境变量\n```bash\n# 检查环境变量\necho $ANDROID_SDK_ROOT\n\n# 如果为空，重新设置\nexport ANDROID_SDK_ROOT=~/Android/Sdk\n```\n\n### 2. \"No target specified\" 错误\n**解决方案**: 确保已安装 Android SDK Platform 31\n```bash\n# 列出已安装的平台\n$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --list\n\n# 安装缺失的平台\n$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager \"platforms;android-31\"\n```\n\n### 3. \"java version not supported\" 错误\n**解决方案**: 确保使用 Java 11+\n```bash\n# 查看当前 Java 版本\njava -version\n\n# 更新到 Java 11+\n```\n\n### 4. Maven 构建内存不足\n**解决方案**: 增加 Maven 内存\n```bash\n# Linux/macOS\nexport MAVEN_OPTS=\"-Xmx2048m\"\n\n# Windows\nset MAVEN_OPTS=-Xmx2048m\n```\n\n### 5. APK 安装失败\n**解决方案**: \n```bash\n# 卸载旧版本\nadb uninstall com.offlinemessenger.android\n\n# 清理 APK 签名冲突\nadb shell pm clear com.offlinemessenger.android\n\n# 重新安装\nadb install -r android/target/offline-messenger-android-1.0.0-SNAPSHOT.apk\n```\n\n## 开发调试\n\n### 查看日志\n```bash\n# 实时查看日志\nadb logcat\n\n# 按标签过滤\nadb logcat *:S OfflineMessenger:V\n\n# 清除日志\nadb logcat -c\n```\n\n### 调试运行的应用\n```bash\n# 列出应用进程\nadb shell ps | grep offline\n\n# 查看应用数据\nadb shell ls /data/data/com.offlinemessenger.android/\n\n# 提取 SQLite 数据库\nadb pull /data/data/com.offlinemessenger.android/databases/messages.db .\n```\n\n## 打包发布版本\n\n### 创建签名密钥\n```bash\n# 生成密钥库 (仅需一次)\nkeytool -genkey -v -keystore offline-messenger.keystore \\\n  -keyalg RSA -keysize 2048 -validity 10000 \\\n  -alias offline-messenger\n```\n\n### 签名 APK\n```bash\n# 使用 jarsigner\njarsigner -verbose -sigalg SHA1withRSA \\\n  -digestalg SHA1 -keystore offline-messenger.keystore \\\n  android/target/offline-messenger-android-1.0.0-SNAPSHOT.apk \\\n  offline-messenger\n\n# 验证签名\njarsigner -verify -verbose -certs \\\n  android/target/offline-messenger-android-1.0.0-SNAPSHOT.apk\n```\n\n## 持续集成 (CI)\n\n### GitHub Actions\n创建 `.github/workflows/build-android.yml`:\n\n```yaml\nname: Build Android APK\n\non:\n  push:\n    branches: [ develop, main ]\n  pull_request:\n    branches: [ develop, main ]\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    \n    steps:\n    - uses: actions/checkout@v3\n    \n    - name: Set up JDK 11\n      uses: actions/setup-java@v3\n      with:\n        java-version: '11'\n        distribution: 'temurin'\n    \n    - name: Setup Android SDK\n      uses: android-actions/setup-android@v2\n    \n    - name: Build with Maven\n      run: mvn clean package -f android/pom.xml\n    \n    - name: Upload APK\n      uses: actions/upload-artifact@v3\n      with:\n        name: offline-messenger-apk\n        path: android/target/*.apk\n```\n\n## 优化和性能\n\n### 减小 APK 大小\n```bash\n# 启用 ProGuard 混淆\nmvn package -Dproguard.enabled=true\n\n# 启用资源压缩\nmvn package -Dres.minify=true\n```\n\n### 快速编译\n```bash\n# 使用增量编译\nmvn compile -pl android\n\n# 跳过测试\nmvn package -DskipTests\n```\n\n## 参考资源\n\n- [Android 官方文档](https://developer.android.com/docs)\n- [Maven Android Plugin](https://simpligility.github.io/android-maven-plugin/)\n- [Android SDK 命令行工具](https://developer.android.com/studio/command-line)\n- [ADB 官方文档](https://developer.android.com/studio/command-line/adb)\n\n## 获取帮助\n\n如遇到问题，请:\n1. 检查日志输出获取详细错误信息\n2. 查看 [Issue](https://github.com/llw77122/OfflineMessenger/issues)\n3. 提交新 Issue 并附加完整的错误日志\n"